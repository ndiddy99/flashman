hidapi.lib built by downloading the repo and changing the runtime library to multithreaded instead of DLL and building a static library
flashman should be able to be built as-is :^)
If you use mac/linux, you should be able to build it by installing hidapi and then writing a simple makefile